@model CITS.EduSuite.Business.Models.ViewModels.UserManualViewModel

@using System.Web.Mvc

@section scripts{
    @Scripts.Render("~/Content/JqueryRepeater")
    @Scripts.Render("~/Scripts/UserManual")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/CitsValidation")
    @Scripts.Render("~/Content/DatePickerJS")
    @Scripts.Render("~/Content/Select2JS")
    @Scripts.Render("~/Scripts/Common")
}

<input type="hidden" id="hdnGetUserManual" value="@Url.Action("GetUserManual", "UserManual")" />
<input type="hidden" id="hdnDeleteUserManual" value="@Url.Action("DeleteUserManual", "UserManual")" />
<input type="hidden" id="hdnDeleteMenuAction" value="@Url.Action("DeleteMenuAction", "UserManual")" />
<input type="hidden" id="hdnFillMenu" value="@Url.Action("FillMenu", "SelectList")" />
<input type="hidden" id="hdnAddEditUserManual" value="@Url.Action("AddEditUserManual", "UserManual")" />



<header class="section-header">
    <div class="row justify-content-between px-1">
        <div class="col-sm-6 col-12">
            <div class="tbl">
                <div class="tbl-row">
                    <div class="tbl-cell">
                        <h3>@EduSuiteUIResources.UserManual</h3>

                    </div>
                </div>
            </div>
        </div>
        <div class="col-6 text-right">
            @*<a id="hdnAddEditUserManual" data-href="@Url.Action("AddEditUserManual", "UserManual")" onclick="Menu.EditUserManualPopup(null)" class="btn btn-success btn-sm "><i class="fa fa-plus-square-o"></i> <span>@EduSuiteUIResources.New @EduSuiteUIResources.BlankSpace @EduSuiteUIResources.Menu</span></a>*@
            <a onclick="UserManual.EditUserManualPopup(null)" class="btn btn-success btn-sm "><i class="fa fa-plus-square-o"></i> <span>@EduSuiteUIResources.UserManual</span></a>

        </div>
    </div>
</header>

<section class="section-content mx-sm-3 mx-0">
    <div class="GridBody">

        <div class="grid-nav-bar form-row justify-content-between">
            <div class="col-lg-3 col-5">
                @Html.DropDownListFor(m => m.MenuTypeKey, new SelectList(Model.MenuTypes, "RowKey", "Text"), EduSuiteUIResources.Select + EduSuiteUIResources.BlankSpace + EduSuiteUIResources.MenuType, new { @class = "input-control form-control form-control-sm form-control-sm", })
            </div>
            <div class="col-lg-3 col-6">
                <input id="searchText" type="text" name="focus" class="form-control gridSearchTextBox" placeholder="@EduSuiteUIResources.Search" /> @* Change Search TextBox Placeholder*@
                <span id="iconSpan" class="iconSpan"><i class="fa fa-times-circle" aria-hidden="true"></i></span>
            </div>

            <div class="col-lg-6 col-2">
                <a class="btn btn-info btn-sm float-right " onclick='window.location.reload()'>
                    <i class="fa fa-refresh"></i>   Refresh
                </a>
            </div>

        </div>

        <div class="table-wrapper">

            <div class="jqGrid_wrapper overflow-auto">
                <table id="grid"></table>
                <div id="pager"></div>
            </div>
        </div>
    </div>
</section>

<script>
    $(document).ready(function () {
        AppCommon.FormatInputCase();
        $("#searchText").on("input", function () {
            UserManual.GetUserManual();
        });

        $("#iconSpan").click(function () {
            $('#searchText').val('');
            UserManual.GetUserManual();
        });

        $("#MenuTypeKey").change(function () {
            UserManual.GetUserManual();
        });
        UserManual.GetUserManual();

    });
</script>
